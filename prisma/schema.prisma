// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model User {
  id           Int     @id @default(autoincrement()) // -> Primary Key med auto optælling
  firstname    String  @default("Ikke navngivet")
  lastname     String  @default("Ikke navngivet")
  email        String  @unique // -> Unik værdi
  password     String
  description  String? // -> Lang tekst
  image        String
  refreshToken String
  isActive     Boolean @default(true) // -> Bool med default true

  testemonies Testemony[]

  @@map("users") // -> Navn på tabel
}

model Product {
  id          Int      @id @default(autoincrement())
  name        String
  slug        String
  image       String
  description String?  // -> Lang tekst
  price       Float    // -> Decimaltal til pris
  stock       Int
  roast       Int
  isActive    Boolean  @default(true)
  createdAt   DateTime @default(now()) // -> Datotid

  @@map("products")
}

model Testemony {
  id        Int      @id @default(autoincrement())
  userId    Int
  content   String?  // -> Lang tekst
  createdAt DateTime @default(now()) // -> Datotid

  user User @relation(fields: [userId], references: [id])

  @@map("testemonies")
}
